- defaults:
    link:
      relink: true

- clean: ['~']

- link:
  ~/.zshrc: :~/.dotfiles/zshrc

  #    ~/.tmux.conf: tmux.conf
  #    ~/.vim: vim
  #    ~/.vimrc: vimrc

- create:
  - ~/hey/you/guys
  - ~/Work

      #- shell:
      #  - [git submodule update --init --recursive, Installing submodules]
